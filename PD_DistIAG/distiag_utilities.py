def run_pd_distiag(training_data, initial_theta, initial_w, rho, M, sigma_theta, sigma_w):

    states_tr = training_data[0]
    next_states_tr = training_data[1]
    rewards_tr = training_data[2]

    return